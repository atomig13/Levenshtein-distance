# Levenshtein-distance
Testing Levenshtein distance calculation between two strings in pandas dataframe

lev-dist_config.py <- python script for lev-dist calculation loop

lev-dist_run.py <- python script to be updated with source csv file for comparison

